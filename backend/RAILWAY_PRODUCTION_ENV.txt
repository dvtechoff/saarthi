# =====================================================
# SAARTHI - COMPLETE RAILWAY ENVIRONMENT VARIABLES
# =====================================================
# 🚀 Copy these EXACT variables to Railway Dashboard → Variables Tab
# All values extracted from your actual project configuration

# =====================================================
# 🐘 DATABASE CONFIGURATION (PostgreSQL)
# =====================================================
# Railway automatically provides this - DO NOT CHANGE
DATABASE_URL=${{Postgres.DATABASE_URL}}

# =====================================================
# 🔐 JWT AUTHENTICATION SETTINGS
# =====================================================
# Values from app/core/config.py and app/core/security.py
JWT_SECRET=saarthi-production-jwt-secret-key-2024-secure-change-me
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080

# =====================================================
# 🚀 APPLICATION CONFIGURATION
# =====================================================
# Values from app/core/config.py
API_V1_STR=/api/v1
PROJECT_NAME=Saarthi Bus Tracker API
DEBUG=False

# =====================================================
# 🌐 CORS CONFIGURATION (CRITICAL FOR FRONTEND ACCESS)
# =====================================================
# Allow admin panel and mobile app origins
CORS_ORIGINS=["http://localhost:5173","http://localhost:8081","http://localhost:19006","https://*.railway.app","https://*.up.railway.app","https://*.vercel.app","*"]

# =====================================================
# 🌐 SERVER CONFIGURATION
# =====================================================
# Railway provides PORT automatically
PORT=8000

# =====================================================
# 👤 DEFAULT ADMIN USER
# =====================================================
# Values from app/main.py - ensure_default_authority()
ADMIN_EMAIL=authority@saarthi.com
ADMIN_PASSWORD=SaarthiAdmin@2024

# =====================================================
# 📊 NEO4J GRAPH DATABASE (REQUIRED)
# =====================================================
# Values from app/core/config.py - Your project uses Neo4j for graph features
# For Production Neo4j AuraDB (Recommended):
NEO4J_URL=neo4j+s://demo.neo4jlabs.com:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=demo

# Alternative: Use a free Neo4j AuraDB instance
# 1. Go to https://neo4j.com/cloud/aura/
# 2. Create free instance
# 3. Replace above values with your instance credentials
# NEO4J_URL=neo4j+s://your-instance-id.databases.neo4j.io
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=your-auradb-password

# =====================================================
# 📱 MOBILE APP INTEGRATION
# =====================================================
# Your mobile app (config/api.ts) expects these test users:
# They are created automatically by seed_data.py:

# Test User Accounts (Auto-created):
# Commuter: commuter@test.com / password123
# Driver: driver@test.com / password123  
# Authority: authority@test.com / password123

# =====================================================
# 🔧 PRODUCTION OPTIMIZATION SETTINGS
# =====================================================
# Database connection pooling
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# Logging configuration
LOG_LEVEL=INFO

# Application timezone (from security.py - uses UTC)
TIMEZONE=UTC

# =====================================================
# 🛡️ SECURITY SETTINGS
# =====================================================
# Refresh token settings (from security.py)
REFRESH_TOKEN_EXPIRE_DAYS=30

# Rate limiting (production mode)
ENABLE_RATE_LIMITING=True

# Security headers enabled (from security.py)
ENABLE_SECURITY_HEADERS=True

# =====================================================
# 🎯 FEATURE FLAGS
# =====================================================
# Enable real-time tracking (WebSocket)
ENABLE_REAL_TIME_TRACKING=True

# Enable graph-based features (requires Neo4j)
ENABLE_GRAPH_FEATURES=True

# Enable automatic data seeding
ENABLE_AUTO_SEED=True

# =====================================================
# 📡 CORS CONFIGURATION
# =====================================================
# Pre-configured in app/core/config.py to include:
# - localhost (development)
# - *.railway.app (production)
# - Mobile Expo origins
# - All origins for testing

# =====================================================
# 📧 EMAIL CONFIGURATION (Future Feature)
# =====================================================
# For notifications (not implemented yet)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# EMAIL_FROM=noreply@saarthi.com

# =====================================================
# 🔄 DEPLOYMENT CONFIGURATION
# =====================================================
# Auto-migration on startup (railway.py)
AUTO_MIGRATE=True

# Auto-seed test data on startup
AUTO_SEED_DATA=True

# Uvicorn server settings (railway.py)
UVICORN_HOST=0.0.0.0
UVICORN_LOG_LEVEL=info

# =====================================================
# 📊 MONITORING & ANALYTICS
# =====================================================
# Health check endpoint: /health
ENABLE_HEALTH_CHECK=True

# API documentation endpoints: /docs, /redoc  
ENABLE_API_DOCS=True

# Request logging (main.py middleware)
ENABLE_REQUEST_LOGGING=True

# =====================================================
# 🚌 BUS TRACKING CONFIGURATION
# =====================================================
# Real-time location update interval (seconds)
LOCATION_UPDATE_INTERVAL=30

# Maximum buses per route
MAX_BUSES_PER_ROUTE=10

# Default occupancy level
DEFAULT_OCCUPANCY=LOW

# =====================================================
# 🗺️ GIS & ROUTING CONFIGURATION
# =====================================================
# Coordinate system (WGS84)
COORDINATE_SYSTEM=EPSG:4326

# Distance calculations (meters)
MAX_STOP_DISTANCE=1000

# Route optimization settings
ENABLE_ROUTE_OPTIMIZATION=True

# =====================================================
# 📱 MOBILE APP URLS (Auto-configured)
# =====================================================
# Your mobile app will use these Railway URLs:
# API: https://your-app.railway.app/api/v1
# WebSocket: wss://your-app.railway.app/socket.io
# Health: https://your-app.railway.app/health
# Docs: https://your-app.railway.app/docs

# =====================================================
# 🎯 DEPLOYMENT VERIFICATION
# =====================================================
# After deployment, test these endpoints:
# ✅ https://your-app.railway.app/health
# ✅ https://your-app.railway.app/docs  
# ✅ https://your-app.railway.app/api/v1/auth/login
# ✅ https://your-app.railway.app/api/routes

# Login with: authority@saarthi.com / SaarthiAdmin@2024

# =====================================================
# ⚠️ IMPORTANT NOTES:
# =====================================================
# 1. Change JWT_SECRET to a unique value for security
# 2. Set up Neo4j AuraDB for graph features
# 3. DATABASE_URL is provided by Railway PostgreSQL service
# 4. All test users are created automatically
# 5. Mobile app URLs update automatically
# 6. CORS is pre-configured for Railway domains